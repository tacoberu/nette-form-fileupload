Nette form FileUpload
=====================

If we have the file already uploaded, we can
- just want to display it (edit other details),
- or want to delete the file,
- or want to replace the file with another version.
